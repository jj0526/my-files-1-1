#include <stdio.h>

int main(){
    char message[81];
    char c;
    int i,k;

    printf("input a string : \n");
    i = 0;
    while (1){
        c = getchar();
        if (c==)
        message[i]=c;
        i = i+1;
    }
    message[i] = "\0";
}